backend:
  name: github
  repo: maxojj/fuwari  # 修改这里
  branch: main             # 你的默认分支名
  base_url: https://decap-cms-oss-oauth.vercel.app

# 媒体文件存放在哪里（Fuwari 默认通常是 public/assets）
media_folder: "src/assets/images"
public_folder: "src/assets/images"

collections:
  - name: "posts"
    label: "文章"
    label_singular: "文章"
    folder: "src/content/posts"
    extension: "md"
    format: "frontmatter"
    create: true
    # 【核心修改点】
    # 这里的 {{slug}} 默认会抓取 title 的内容。
    # 但在 Sveltia 界面中，你可以手动覆盖这个 slug。
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    
    # 告诉 CMS 列表里显示标题字段，方便你找文章
    identifier_field: title 
    
    fields:
      - { label: "标题 (网页显示的中文标题)", name: "title", widget: "string" }
      - { label: "发布日期", name: "published", widget: "datetime", format: "YYYY-MM-DD" }
      - { label: "描述", name: "description", widget: "string", required: false }
      - { label: "封面图片", name: "image", widget: "image", required: false }
      - { label: "标签", name: "tags", widget: "list", required: false }
      - { label: "分类", name: "category", widget: "string", required: false }
      - { label: "草稿", name: "draft", widget: "boolean", default: false }
      - { label: "正文内容", name: "body", widget: "markdown" }


